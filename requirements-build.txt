# Toolchain for CMake/Ninja/Conan in a Python venv (`pip install -r requirements-build.txt`).
# Pinned to PyPI-current versions as of 2026-05; bump with: pip index versions cmake ninja conan

cmake==4.3.2
ninja==1.13.0
conan==2.28.1
